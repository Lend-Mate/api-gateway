networks:
  lendmate-net:
    external: true

services:
  api-gateway:
    image: "api-gateway"
    build: .
    container_name: api-gateway
    restart: unless-stopped
    networks:
      - lendmate-net
    environment:
      JAVA_OPTS: "-Xmx150m -Xms96m"